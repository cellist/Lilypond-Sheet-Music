ve = \relative c' {
   \voiceconsts

   d4->\f a-> h-> cis->
   d4.-> d8->~ d4 d->
   g,-> c?-> h-> b->
   a4.-> a8->~ a4 a->
   a-> a' a,-- as--
   g g' g, g'

   c,8 r f2-> f4~
   f g,-> c-> r \bar "||"
   c->(\p g') e-. es-.
   d \acciaccatura fis8 g4->~ g8 g,-. a-. h-.
   c4->( g') e-. es-.
   d \acciaccatura fis8 g4->~ g8 g,-. a-. h-.
   c4(-> a') e\( es

   d h'8 b->~ b4\) g--
   fis d e fis
   g2~ g8 g,-. a-. h-.
   c4->( g') e-. es-.
   d \acciaccatura fis8 g4->~ g8 g,-. a-. h-.
   c4->( g') e-. es-.

   d \acciaccatura fis8 g4 g,8 g'( fis! f
   e4)-> b' c, b'
   f, a' c, a'
   \appoggiatura { d16[ dis] } e4-> dis!8( c dis4 h?)
   c es,( d-- g--)
   \repeat volta 2 {
     c,2(->_\pifo g)
     \appoggiatura { h16[ c] } cis2(-> g)
     \appoggiatura { c16[ cis] } d2(-> g,)

     \acciaccatura fis'8 g4-> g,-- a-- h--
     c2(-> g)
     \appoggiatura { h16[ c] } cis2(-> g)
     \appoggiatura { c16[ cis] } d2(-> g,)
     \acciaccatura fis'8 g4-> g,-- a-- h--
     c2 g
     e c'
     f, c'
     a f
     a\< d\!

     a d
     g,4-> a'-. g-. f-.
     e-.\> d-. c-. h-.\!
     c2(-> g)
     \appoggiatura { h16[ c] } cis2(-> g)
     \appoggiatura { c16[ cis] } d2(-> g,)
     \acciaccatura fis'8 g4-> g,-- a-- h--
     c2 g
     e c'
     f\< c\!

     f\> c\!
     d4-> a-> h-> cis->
     d4.-> d8->~ d4 d->
     g,-> c?-> h-> b->
     a4.-> a8->~ a4 a->
     a-> a' a,-- as--
     g g' g, g'
   }
   \alternative {
     { c, e-> f-> g-> | gis-> a-> b-> h-> }
     { c,8 r f2-> f4~ | f g,-> c-> r }     
   }
   \bar "|."
}